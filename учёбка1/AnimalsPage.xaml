<Page x:Class="учёбка1.AnimalsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:учёбка1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AnimalsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*" />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Row="0" Grid.ColumnSpan="2" x:Name="AnimalsGrid"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Название:" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <TextBox x:Name="NameTextBox" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Количество:" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <TextBox x:Name="AmountTextBox" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" />
        <Button x:Name="AddButton" Grid.Row="3" Grid.ColumnSpan="2" Content="Добавить Животное" Click="AddButton_Click" />
    </Grid>
</Page>
